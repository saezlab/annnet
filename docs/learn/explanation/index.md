# Explanations

Deep dives into how annnet works under the hood.

- Interoperability: runtime backend adapters (NetworkX/igraph/graph‑tool) and conversion manifests.
- annnet zero‑loss serialization: `.annnet` storage with Zarr + Parquet + JSON.

See also the architecture overview for broader design context.
